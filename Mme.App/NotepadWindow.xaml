<Window x:Class="Mme.App.NotepadWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Notepad" Width="520" Height="420"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="{DynamicResource ThBg}" Foreground="{DynamicResource ThLabel}"
        FontFamily="{DynamicResource ThFont}">
  <Grid Margin="8">
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/><RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <TextBox x:Name="TxtNote" AcceptsReturn="True" AcceptsTab="True"
             TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
             FontFamily="Consolas"/>
    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right"
                Margin="0,6,0,0">
      <Button Content="Copy" Width="70" Click="Copy_Click"/>
      <Button Content="Save As..." Width="84" Margin="6,0,0,0" Click="Save_Click"/>
      <Button Content="Close" Width="70" Margin="6,0,0,0" Click="Close_Click"/>
    </StackPanel>
  </Grid>
</Window>
