# PARITY LEDGER

One row per VB6 procedure. States: **Ported** (with tests), **Skipped (UI)**,
**Skipped (BCL)**, **Skipped (dead code)** — every skip has a reason.
Rule: nothing gets marked Ported without (a) the VB6 body read in-session,
(b) a `// VB6:` header on the C# method, (c) at least one test.

## modSyntaxsFunc.bas — 37 procedures (Phase 1a — COMPLETE)

| VB6 procedure | State | C# location | Notes / quirk pins |
|---|---|---|---|
| RemoveDuplicateNumbersFromString | Ported | TextUtils | No-comma path returns CStr(Val()); parts trimmed, NOT normalized; all-empty → "0" |
| GetOwner | Skipped (UI) | — | Win32 GWL_HWNDPARENT → WPF `Window.Owner` |
| SetOwner | Skipped (UI) | — | Same |
| SetTopMostWindow | Skipped (UI) | — | WPF `Window.Topmost` |
| in_long_arr | Ported | TextUtils.InLongArray | Null array → false (VB6 error path parity) |
| in_str_arr | Ported | TextUtils.InStringArray | Ordinal (Option Compare Binary); VB6 16-bit index limit lifted |
| ClearListViewSelections | Skipped (UI) | — | WPF selection binding |
| ExtractNumbersFromString | Ported | TextUtils | PINS: "1-2"→12; ".5"→5; lone '-' reset |
| ExtractValueFromString | Ported | TextUtils | Case-insensitive find; space/'*' skip; overflow → 0 |
| GetFirstWord | Ported | TextUtils | PIN: any LEADING space → "" (space search on untrimmed input) |
| FileExists | Skipped (BCL) | — | `File.Exists` |
| FolderExists | Skipped (BCL) | — | `Directory.Exists` |
| HandleError | Skipped (UI) | — | VB6 msgbox/terminate flow → .NET exceptions. Each ported method documents its VB6 error-path default-return where observable |
| NumberKeysOnly | Ported | TextUtils | Sequential rule order preserved (46-without-decimal → 0; 45 restored after zeroing) |
| PutCommas | Ported | TextUtils | PIN: blank input returned untouched; bShorten "T" format ≥1e12; sign/frac preserved |
| FormatWithCommas | Ported | TextUtils (decimal/double/long overloads) | VB6 Format$ banker's rounding pinned (0.5→"0", 1.5→"2", 2.5→"2"); double overload mirrors CDec-fallback |
| AutoPrepend | Ported | TextUtils | Blank-prefix skip uses spaces-only Trim |
| AutoAppend | Ported | TextUtils | Same |
| RemoveCharacter | Ported | TextUtils | PIN: multi-char/empty sChar removes nothing |
| RemoveVowles | Ported | TextUtils.RemoveVowels | Original name is a typo (sic). First char always kept; lowercase-only removal |
| RoundUp | Ported | TextUtils | Int()-floor based ceiling: RoundUp(-2.5) = -2 |
| RoundUpTo5 | Ported | TextUtils | C# `%` matches VB6 Mod on negatives (-6 → -5) |
| UnloadForms | Skipped (UI) | — | VB6 Forms collection → WPF window lifetime |
| FormIsLoaded | Skipped (UI) | — | Same |
| PutCrLF | Ported | TextUtils.PutCrLf | **PINNED VB6 BUG**: single char after final LF is dropped ("a\nb" → "a\r\n"); existing CRLF gains extra CR; 16-bit index limit lifted |
| AutoComplete | Skipped (UI) | — | ComboBox SendMessage COM → Phase 3 WPF behavior / WPF-UI AutoSuggestBox |
| IsAlphaNumeric | Ported | TextUtils | ASCII `Like "[0-9A-Za-z]"`; empty → false |
| IsAlphaBetical | Ported | TextUtils.IsAlphabetical | Optional space allowance |
| RegExpFind | **Skipped (dead code)** | — | ZERO live call sites in the VB6 codebase; contains scalar-to-array-function assignments that could never execute sanely. Do not resurrect — use RegexFindV2 |
| FindStringIndex | Ported | TextUtils | vbTextCompare → OrdinalIgnoreCase (ASCII data); PIN: null/OOB error path → 0, not -1 |
| RegExpFindv2 | Ported | RegexUtils.RegexFindV2 | No-match sentinel {("",{""})} preserved (7 live call sites depend on it); empty-submatch filtering + allowEmpty flag; module's own worked example is a test |
| EscapeRegex | Ported | RegexUtils | PIN: escapes ONLY ( ) [ ] . $ ^ — never substitute Regex.Escape() |
| SetClipboardText | Skipped (UI) | — | Phase 2 ClipboardService (keep the 4-retry/250ms semantics) |
| SortLettersWithSeparator | Ported | TextUtils | Empty segments preserved |
| SortLetters | Ported | TextUtils | Ordinal char sort == VB6 binary bubble sort; ("" → "" here; VB6 errored) |
| FormatBigIntWithCommas | Ported | TextUtils (+ string/double/decimal overloads) | Scientific-notation input handled; truncates toward zero; "-0" collapses |
| Truncate | Ported | TextUtils | Fix()-based; IEEE754 scaling artifacts identical (1.005,2 → 1.0 pinned) |

**Private helpers ported with their parents:** ToPlainIntegerString, KeepDigits,
InsertThousands (inside TextUtils, private).
**Types:** RegexMatches → `RegexMatchV2`; ResizeCons/RECT/TITLEBARINFO skipped (UI/Win32).
**Module constants:** MaxULong/MaxLong/IntOffset/MaxInt → `VbRuntime`; Win32
window-state constants skipped (UI).

## modMMudFunc.bas — 70 procedures (Phase 1b — COMPLETE, 70/70 accounted)

Dependency audit is run per proc before porting (grep body for `nGlobal`,
`tab[A-Z]`, `bGreaterMUD`, `nNMRVer`); the audit result is recorded here.

| VB6 procedure | Deps found | State | C# location | Notes / quirk pins |
|---|---|---|---|---|
| GetArmourTypeEnum | clean | Ported | EnumNames | 3–6 collapse to "Leather" |
| GetWeaponTypeEnum | clean | Ported | EnumNames | |
| GetClassWeaponTypeEnum | clean | Ported | EnumNames | |
| GetWornTypeEnum | clean | Ported | EnumNames | PIN: 4/13 both "Finger", 14/17 both "Wrist" |
| GetItemTypeEnum | clean | Ported | EnumNames | PIN: unknown → BARE number (no wrapper) |
| GetCostTypeEnum | clean | Ported | EnumNames | |
| GetSpellTargetsEnum | clean | Ported | EnumNames | |
| GetShopTypeEnum | clean | Ported | EnumNames | Long param |
| GetMonAttackTypeEnum | clean | Ported | EnumNames | |
| GetMonTypeEnum | clean | Ported | EnumNames | |
| GetMonAlignmentEnum | clean | Ported | EnumNames | |
| GetMageryEnum | clean | Ported | EnumNames | PIN: non-zero (incl. unknown) gets "-level" suffix; omitted level → "-0" |
| SpellAttackTypeEnum | clean | Ported | EnumNames | PIN: unknown → bare number in both modes |
| GetHitMin | bGreaterMUD ×1; calls GetClassArmourType (tabClasses) | Ported | IGameEngineRules.HitMin | DB lookup externalized: caller passes resolved ArmourType (null ⇔ VB6 nClass=0; lookup-miss ⇔ pass 0, still subtracts) |
| GetHitCap | bGreaterMUD ×1 | Ported | IGameEngineRules.HitCap | 99/100 |
| GetSpellHitCap | bGreaterMUD ×1 | Ported | IGameEngineRules.SpellHitCap | 98/100 |
| GetDodgeCap | bGreaterMUD ×2 | Ported | IGameEngineRules.DodgeCap | PIN: softCap IGNORED on stock (95); GMUD 55/98. nClass param dropped — VB6 class logic commented out 2025-09-24 |
| CalcExpNeeded | bGreaterMUD, nGlobalDatVer ×2 | Ported | IGameEngineRules.ExpNeeded | Dispatcher: GMUD datver >0 and ≤1.85 → 1_8_5, else new; stock → STOCK. Returns double (VB6 dispatcher type) |
| CalcExpNeeded_STOCK | clean | Ported | ExpTables.CalcExpNeededStock | Currency → decimal; uint32-rollover emulation preserved; L1=0, L2=table×10; hand-traced anchors L3–L7 |
| CalcExpNeeded_GMUD | clean | Ported | ExpTables.CalcExpNeededGmud | Double; I64 guard; taper cliff 34, floor 108; PIN: L1 = chart×10 (NOT 0); source-comment golden L2(290)=2900 |
| CalcExpNeeded_GMUD_1_8_5 | clean | Ported | ExpTables.CalcExpNeededGmud185 | Flat 115/109/108 bands; identical to new formula through L39, diverges L40+ (tested) |
| GetExpModifiers_STOCK | clean (Private) | Ported | ExpTables (private) | Case Else splits at 26: (50,40) / (23,20) |
| GetExpModifiers_GMUD | clean (Private) | Ported | ExpTables (private) | Levels 1–32; out-of-range → (0,0) faithful |
| IDiv | clean (Private) | Ported | ExpTables (private) | Fix(a/b); b=0 → 0 |
| CanI64Mul | clean (Private) | Ported | ExpTables (private) | I64_MAX literal preserved exactly |
| GMUD_DiminishingReturns | clean | Ported | MudMath.GmudDiminishingReturns | Kept as named func, NOT a rules member (VB6 call sites invoke the GMUD variant explicitly); triangular anchors tested |
| RoundUpToNearest5 | clean | Ported | MudMath.RoundUpToNearest5 | `\` trunc-toward-zero semantics; cross-checked ≡ TextUtils.RoundUpTo5 over −50..50 |
| CalcDodgeVSAccuracy | bGreaterMUD ×1; calls GetDodgeCap | Ported | IGameEngineRules.DodgeVsAccuracy | Stock: `\8` / `\tempAccy`; GMUD: CLng banker's on both Long assignments, softcap 55 + DR(x,4), cap 98. Anchors: stock (50,100)=41; GMUD (50,100)=35, (100,100)=79 |
| CalcDodge | clean | Ported | CharacterMath.CalcDodge | PIN: nPlusDodge Double added to Integer accumulator → banker's (2.5→18, 3.5→20 tested); negative clamp commented out in VB6 — kept off |
| DodgeMaxAccForPercent | bGreaterMUD ×1 | Ported | IGameEngineRules.DodgeMaxAccuracyForPercent | Stock closed-form k=raw·10\pct + nudge loops; GMUD upper-mid binary search hi=1000; −1 unattainable. PIN: nClass param dropped — dead in VB6 body |
| CalcEncum | UI: frmMain.lblDatVer | Ported | CharacterMath.CalcEncum | EXTERNALIZED UI READ → isV111iData param (Phase 1e GameDataVersion supplies it). Bonus step CLng banker's; trailing Round(x,0) is identity on Long |
| CalculateBackstabAccuracy | bGreaterMUD ×1 | Ported | IGameEngineRules.BackstabAccuracy | Stock: Fix halves, +5 class / −15 race-only; GMUD: whole RHS Double → CLng banker's (50.5→50, 51.5→52 tested), STR<StrReq −15 |
| CalculateAccuracy | bGreaterMUD ×6; calls GetClassCombat (tabClasses) | Ported | CombatMath.CalculateAccuracy | EXTERNALIZED DB → classCombat param (CombatLVL−2; miss→1), consumed only when nClass>0. PINs: pity accy gate `worn=0 And (stock Or plusAccy=0)`; stock odd-number penalty Fix(x/2)·2; stock-only sqrt correction loop; per-stat gmud/bash gates; raw-concat " *smash/bash" text quirk; GMUD smash ×3/2 |
| GetClassCombat | tabClasses (modMMudDatabase.bas) | Externalized | — | Phase 1e; contract documented on classCombat param |
| CalculateAttackDefense | bGreaterMUD ×3; calls GetHitMin/GetHitCap/CalcDodgeVSAccuracy | Ported | CombatMath.CalculateAttackDefense | **PIN: colon-chained lower clamps are DEAD CODE** (single-line Ifs) — only upper clamps live, do not "fix". Evil tiers via EvilPoints enum (banker's-rounded VB6 Double literals, "Villian" typo kept); stock backstab dodge `\5`; GMUD vileward evil gates ≤Seedy→0 / ≤Criminal→½ then `\10` |
| GetAbilityStats | UI + DB heavy | **Ported (session 48)** | Mme.Data/SpellEqService.cs (AbilityStats) | Full port: DR tenths, spell/ability/class/monster/item name branches, the no-header set (1/8/17/18/19/140/141/148), the 148-with-only-"cast N" textblock substitution, and the 43/153/151 nested-PullSpellEQ brackets. ListView rows externalized to a line list |
| CalcMoneyRequiredToTrain | clean | Ported | CharacterMath.CalcMoneyRequiredToTrain | Currency; Fix then ·10 |
| CalcMovementSpeed | bGreaterMUD ×1 | Ported | IGameEngineRules.MovementSpeed | Shared `out:` floor 1000 both engines; GMUD (pct/100)²·2000 → CLng banker's (encum 33 → 1318 tested) |
| CalcRestingRate | bGreaterMUD ×1 | Ported | CharacterMath.CalcRestingRate | Divisor 750/500 via rules. **PIN: VB6 Long overflow (error 6) → −1** — emulated with int-range checks on every Long intermediate |
| CalcBSDamage | bGreaterMUD ×1 | Ported | CombatMath.CalcBsDamage | PIN: (Level+100)% multiplier gate is `classStealth Or Not GMUD` — race-only GMUD backstabber skips it (61 vs 73 tested) |
| CalcManaRegen | bGreaterMUD ×1 | Ported | CharacterMath.CalcManaRegen + IGameEngineRules.ManaRegenBonus | Kai early-return base 1; meditating exits pre-bonus; stock/GMUD bonus forms diverge for negative MPRegen (13 vs 14 tested) despite "functionally equivalent" source comment |
| CalcMR | clean | Ported | CharacterMath.CalcMr | Fix((INT+WIS·3)/4)+mods |
| CalcMaxHP | clean | Ported | CharacterMath.CalcMaxHp | |
| CalcMaxMana | clean | Ported | CharacterMath.CalcMaxMana | |
| CalcSpellCasting | clean | Ported | CharacterMath.CalcSpellCasting | Per-magery blends; Kai base 500; None/Else → 0 |
| GetEncumPercents | clean | Ported | CharacterMath.GetEncumPercents | **PIN: faithful For x=0.1 To 0.9 Step 0.1 Double drift** — 8th x is 0.7999…, so 4800 → "80% @ 3840" not 3841 (tested); labels via 15-sig-digit CStr so 0.3000…04 prints "30" |
| CalcPicklocks | bGreaterMUD ×1 | Ported | IGameEngineRules.Picklocks | PIN: GMUD `/` divisions banker's-ROUND on Long assign (494/7→71 tested) — the C# reference comment in the VB6 source truncates; the code wins. Stock ignores CHA |
| CalcCPLevel | clean (plain `Function` — missed by Public/Private grep) | Ported | CharacterMath.CalcCpLevel | Σ Fix(i/10)·5+10 |
| CalcTrueAverage | calls MaxSwings rule | Ported | CombatMath.CalcTrueAverage | Swings ≤0 → −1; cap via IGameEngineRules.MaxSwings; Round(x,2) banker's |
| GetQuickAndDeadlyBonus | UI + DB wrapper | Skipped | — | Thin wrapper over CalcQuickAndDeadlyBonus reading form controls; Phase 3 VM |
| CalcQuickAndDeadlyBonus | bGreaterMUD ×1, nGlobalDatVer ×1 | Ported | IGameEngineRules.QuickAndDeadlyBonus | Stock: encum>66 gate, cap 20, Fix-halve at encum≥33; GMUD: divisor 40 (DatVer>1.85) / 50, Currency→Integer banker's on energyRemain (100.5 EU tested) |
| CalcEncumbrancePercent | clean | Ported | CharacterMath.CalcEncumbrancePercent | Currency clamps then Fix |
| AdjustSpeedForSlowness | clean | Ported | CombatMath.AdjustSpeedForSlowness | Fix(Speed·3/2) |
| CalculateStealth | bGreaterMUD ×2 | Ported | CharacterMath.CalculateStealth | **PIN: GMUD stat sum is banker's-rounded FLOAT SUM but breakdown labels print stock Fix values** — label can disagree with math by 1 (72 vs 73 tested). GMUD encum penalty Fix(pct·15/100) clamp [0,15] |
| CalcEnergyUsed | clean | Ported | CombatMath.CalcEnergyUsed | 2025-03-16 simplified form; Currency `/` → Double then Fix per step; encum=−1 optional-skip; backstab skips speedAdj |
| CalcEnergyUsedWithEncum | clean | Ported | CombatMath.CalcEnergyUsedWithEncum | Currency encum variant of the inline Integer step |
| AdjustEnergyUsedWithSpeed | clean | Ported | CombatMath.AdjustEnergyUsedWithSpeed | |
| AdjustEnergyUsedWithEncum | clean | Ported | CombatMath.AdjustEnergyUsedWithEncum | |
| GMUD_GetSpDmgMultiplierFromSC | clean | Ported | MudMath.GmudGetSpDmgMultiplierFromSc | 0 below 150; (sc−100)\50; named-static like GmudDiminishingReturns |
| GetSpellCastChance | tabSpells lookup path | Ported (pure path) | SpellMath.GetSpellCastChance | EXTERNALIZED DB: caller resolves Diff/Magery=5→kai, passes fromSpellLookup=true so the all-zero guard is bypassed (resolved spell with Diff 0 + SC 0 → 100, not 0). Kai hard cap 100; engine cap via rules.SpellHitCap. Integer-overflow error path not replicated (noted) |
| ResistPct_SignedOfBase | clean | Ported | SpellMath.ResistPctSignedOfBase | TOL 1.0; CLng(Round(x)) banker's |
| NegResistPct_ShareOfTotal | clean | Ported | SpellMath.NegResistPctShareOfTotal | Source-comment golden (75,−30)→−29 tested |
| CalculateResistDamage | clean | Ported | SpellMath.CalculateResistDamage | Currency semantics: every nDamage mutation is a Double expr stored via CCur (4dp banker's); bonusResist step has Fix, sub-50-MR boost step does NOT; anti dr Fix(mr/2) cap 75 / mr>51 dr Fix((mr−50)/2) cap 50; PIN dead zone mr∈{50,51}; totalResist Fix(mr/2) cap 98 gated type=2 or (anti ∧ type=1); final Round banker's 0dp (73.5→74, 70.5→70 tested) |
| Module types/enums | — | Ported (partial) | Model/GameEnums.cs | eAttackTypeMUD, eEvilPoints (**PIN: Double literals 29.99 etc. banker's-round on enum-Integer coercion → 30/40/80/120/300; "Villian" typo kept**), enmMagicEnum (modMMudDatabase.bas). tCharacterProfile/tCombatRoundInfo/tSpellCastValues deferred to their consumer procs |
| CalcCombatRounds | bGreaterMUD ×1 (regen window) | Ported | CombatMath.CalcCombatRounds | Per-mob HP CLng banker's, but surprise block recomputes pure-Double (PIN); regen re-estimate uses full mutated mobHealth — no 0.5-ceil, no ×numMobs (PIN); 1.5-bump only when Rtk == 1 exactly; −Int(−x·2)/2 ceil-to-0.5; RTK/RTD windows [1,200] else "<infinitely attacking>"/"<unfazed by damage>"; success Round(RTD²/(RTK²+RTD²)·100), ≥95/≥5 string tiers; multi-mob label RTC |
| ccr_Saturate / ccr_SmoothStep / ccr_Lerp / ccr_SafeDiv / ccr_Max | clean | Ported (private) | CombatMath (private statics) | Only consumed by the surprise-attenuation block |
| ccr_Min | clean | Skipped (dead) | — | Zero call sites in the VB6 tree |
| ExtractTextCommand | clean | Ported | MudParse.ExtractTextCommand | **PIN: `While x < Len` drops the LAST character**; comma with empty buffer is appended, comma after content terminates; no space → whole input returned |
| ExtractMapRoom | clean | Ported | MudParse.ExtractMapRoom | ExitType numeric-0 default coerces to "0"; Val() saves the deliberate Mid-length overshoot; slash-at-start/no-slash/trailing-slash → error-5 handler defaults (0/0/"0"); trailing space → ExitType "" overrides the "0" |
| TestPasteChar | clean | Ported | MudParse.TestPasteChar | LCase single-char whitelist a–z 0–9 ( ) - _ , : space ' " . ` |
| TestAlphaChar | clean | Ported | MudParse.TestAlphaChar | |
| GetAbilityName | bGreaterMUD ×4 | Ported | EnumNames.GetAbilityName | 187-case stock + GMUD block. **PIN: duplicate `Case 1101` — "MeetsReqToHit" wins, "UseSpell" is dead; `Case 1102` missing → "Ability 1102"**; message abils (101/115/120/137/144/155/1116) + GMUD QuestFlag 191–199/219/223–400 empty unless bForceAll; stock > 187 → "Ability n" |
| GetAbilityList | bGreaterMUD ×1, bHideRecordNumbers | Ported | EnumNames.GetAbilityList | hideRecordNumbers externalized as a parameter; nMax 1120 GMUD / 200 stock; index 0 = ""; empty-name ≤ 200 → "[Ability n]", > 200 → "" |
| AbilityEffectsCharStats | bGreaterMUD ×2 | Ported | EnumNames.AbilityEffectsCharStats | Whitelist HashSets (shared / stock-only 15,16 / GMUD-only 1001–1113); **PIN: local bForceAll is declared but never set — message abils always excluded**; abils 1 and 17 deliberately excluded |
| SpellIsInGame | tabSpells, nNMRVer | Ported | SpellMath.SpellIsInGame | Record externalized; NMR ≥ 1.8 Classes-string rescue for orphan spells; Kai autolearn (Magery 5, ReqLevel ≤ level) unless disableKaiAutolearn |
| SpellIsUsable | tabSpells, class magery, nNMRVer | Ported | SpellMath.SpellIsUsable | Class magery/level externalized (classMagery, classMageryLvl). **PIN: nClass < 1 → True; the Learnable>0 ∧ Magery=0 rescue inside the magery-mismatch branch is DEAD (mismatch requires Magery ≠ 0) — kept verbatim**; Magery-0 spells route to the Classes list (enforced NMR ≥ 1.7 only); align abils 97/98/110–112; abil 1107 no-op (consumer commented out in VB6) |
| GetCurrentSpellMinMax | **modMMudDatabase.bas**, GetMaxLevel | Ported | SpellMath.GetCurrentSpellMinMax | Lives in the DB module but is pure given the record — pulled forward into wave 3. GetMaxLevel externalized as maxLevel = 255. Formula path builds "base+(inc*lvl)" strings, sets ByRef noHeader; useLevel ByRef downgraded when nothing scales; bonus multiplies min/max via Fix but NEVER duration (PIN); **PIN: the returned struct's NoHeader FIELD is never assigned by VB6 — only the ByRef param**. **C# ref params (useLevel, noHeader) moved ahead of the optionals vs VB6 order** — documented in the doc header; discard overload provided |
| CalcRoundsToOOM | **modMain.bas** | Ported | SpellMath.CalcRoundsToOom | Pure despite living in modMain — pulled forward. Aura pre-check regenBetween = CLng(regenTicks·regen) vs cost + (cost/2)(1−chance/100); fail-refund Fix(cost/2) on the accumulated schedule ALSO resets the aura recast counter (doubles recast rate); **PIN: precheck, round>200-at-full-mana, and rounds=999 all leave the Integer default → 0 (behaviorally identical never-OOM exits)** |
| CalculateSpellCast | tabSpells ×81, bGreaterMUD ×6, GetMaxLevel, tCharacterProfile | Ported | SpellMath.CalculateSpellCast | Record + maxLevel externalized; consumes only 5 profile fields (SpellDmgBonus, Spellcasting, MaxMana, SpellOverhead, ManaRegen). **PINs: (a) abils 8/18/176 with AbilVal ≠ 0 ASSIGN the accumulators (overwrite, not add); (b) abil 17 with AbilVal ≠ 0 reuses the max-damage resist temp for the min add; (c) nDamageResisted accumulates points in the loop then is OVERWRITTEN with ResistPct_SignedOfBase percent at the end (drain-overwrite case yields −233% tested); (d) elemental resistance hits damage only for abil 1 but damage+min for abil 17 — MinRoundDmg can exceed AvgRoundDmg (tested); (e) GMUD gates: abil 8/18 bSpellValueModified GMUD-only, abil 176 GMUD-only; (f) `GetAbilityStats(n, 0, , False)` collapses to GetAbilityName — verified against its body, GetAbilityStats itself stays Phase 3** |
| GetAbilityStats | ListView UI + GetAbilityName | **Ported (session 48)** | Mme.Data/SpellEqService.cs (AbilityStats) | The nValue=0 path still ≡ GetAbilityName (so CalculateSpellCast's use is unchanged); the value-bearing branches are now all rendered. bPercentColumn dropped (chose a ListView column; meaningless once the list is externalized) |
| CalculateAttack | tabItems (WeaponRecord: 9 fields + Abil/AbilVal ×20), PullSpellEQ + GetSpellName, frmMain.lblInvenCharStat(10).Tag UI read, nGlobalChar* session globals, GetClassCombat/GetClassStealth/GetRaceStealth, GetMaxLevel, bV111iData, bGreaterMUD ×13, nGlobalDatVer | Ported (wave 4, full 779-line read) | AttackMath.CalculateAttack | SEAMS: weapon record externalized (null / unlisted negative weaponNumber ≡ failed Seek → empty result); loadedState DTO replaces the nGlobalChar* globals (null ≡ fresh zeroed session); classCombat / classStealthFromClass / raceStealthFromRace are pre-resolved lookups; uiAccuracyFallback replaces the label-Tag read; castDescription delegate must return `GetSpellName(n, bHide) & ", " & PullSpellEQ(True,0,n,,,,True,,,,,SpellDmgBonus)` — null skips the build (≡ empty sCasts) while the Abil 43/114/1114 ordering logic stays ported. PINS: (a) proxies −2..−5 skip bAbil68Slow and leave sAttackDesc empty ("backstab with "); (b) the %spell variable is SHARED with the stock negative-dodge blend — an Abil 43 with no preceding Abil 114 can pick up the leftover blend fraction as its percent; (c) surprise/smash nSwings=1 is immediately recomputed (order fidelity); (d) CalcEnergyUsed backstab arg is unreachable-True, kept verbatim; (e) GMUD MA damage formulas banker's-round (Long = Double expr) while stock uses Fix chains; (f) stock kick/jk use PRE-roll 1.33/1.66 (before DR), GMUD uses POST-DR multipliers + accy −10/−15; (g) stock DR subtracts BEFORE the damage multiplier, GMUD after; (h) multi-match cast parsing overwrites nExtraAvgHit/Swing per match and the final divide averages only the LAST match over the match count; nCount=0 leaves the previous match's value; (i) on the manual-damage path VB6 reads a STALE tabItems cursor in the casts build — C# requires the record and skips the build when absent; (j) crit diminishing returns: stock 40+Fix((c−40)/3) cap 99, GMUD hard cap 65; (k) the tabItems re-Seek inside the ability loop is DB-cursor housekeeping with no C# analogue — dropped |
| GetAbilityStatSlot (nEquip half) | **modMain.bas**; sText half calls GetAbilityStats (UI) | Ported (partial) | AttackMath.GetAbilityEquipSlot | Pure ability → equip-slot switch; default −1 set before the Select (Case 0 no-op keeps it); commented-out cases (9 shadow, 28, 38 tracking, 39 thievery, 72 damageshield) return −1; the sText/GetAbilityStats half stays Phase 3 |
| tAttackDamage / WeaponEquipStats / LoadedCharState | — | Ported | Model/AttackTypes.cs | tAttackDamage verbatim; WeaponEquipStats/LoadedCharState are the externalized nGlobalChar* session globals |
| tCharacterProfile / tCombatRoundInfo / tSpellCastValues / RoomExitType | — | Ported | Model/CharacterProfile.cs, Model/CombatTypes.cs, Model/SpellTypes.cs | MA arrays 1-based length-4 convention kept; RoomExit.ExitType default "0" quirk; SpellCastValues.DamageResisted is the final signed PERCENT |

**Exp/spell/attack anchors are hand-traced algorithm pins** — independent
VB6-runtime CSV goldens still required per strategy §8.2 (run the VB6 dump on
Windows, commit as tests/goldens/, upgrade the tests). modMMudFunc is COMPLETE
(70/70). GetAbilityStats remains Phase 3 (ListView-bound; its nValue=0 collapse
serves CalculateSpellCast, its sText half serves GetAbilityStatSlot). PullSpellEQ
(modMMudDatabase) remains for Phase 1e — until then CalculateAttack callers
supply the castDescription delegate or skip the casts build. Pulled-forward
out-of-module pure procs so far: GetCurrentSpellMinMax (modMMudDatabase.bas),
CalcRoundsToOOM + GetAbilityStatSlot-nEquip (modMain.bas).

## clsMonsterAttackSim.cls — 13 members (Phase 1c — COMPLETE, 13/13 accounted)

Provenance note: src/Mme.Core/Sim/MonsterAttackSim.cs pre-existed this phase with
no log/ledger trail. Per §0 it was treated as UNVERIFIED and audited line-by-line
against a fresh full read of clsMonsterAttackSim.cls (1712 lines) in Session 6.
Verdict: faithful. All rows below reflect that audit, not trust in the file.

| Member | deps | status | C# location | pins / notes |
|---|---|---|---|---|
| Public fields / properties | — | Audited-ported | Sim/MonsterAttackSim.cs | Property set verbatim incl. HitMin/HitCap; PIN (dead colon-clamp): the `If x > 100` clamp sits INSIDE the `If x < 0 Then` colon-statement — a >100 assignment is stored unclamped (readback anchor: HitMin −5→0, HitCap 150 stays 150). DEVIATION (doc'd): ctor pre-applies ResetValues defaults + convenience defaults CombatLogMaxRounds=10, DynamicCalcDifference=0.0001 — header pin added Session 6 |
| RandomNumber | Rnd | Audited-ported | RandomNumber | `Int((upper−lower+1)·Rnd+lower)`; SEAM: RandomSource Func<double> [0,1) replaces Rnd (injectable; ScriptedRandom in tests validates exact draw counts) |
| AddToCombatLog | — | Audited-ported | AddToCombatLog | Round-header + append; respects CombatLogMaxRounds; PIN: stale-x "attack 6" label bug preserved (loop var reused after exit → sixth-slot label regardless of actual slot) |
| GetMaxDamage | — | Audited-ported | GetMaxDamage | Nothing-to-sim → 0; resistable spells count HALF energy cost; duration spells add full tick damage × rounds; PIN: zero-energy attack contributes 0 (division guard path); PIN: between-round-only configs → 0 (loop only walks nAtk slots) |
| RunSim | RandomNumber, IsSpellResisted, CalcResistedDamage, ResetActive* | Audited-ported | RunSim | 670-line core loop. PINS: (a) next_attempt energy check tests nLastAttackType=1 (normal attack) despite the 'spell comment — fires after successful NORMAL attacks too; (b) nResist_Reduction is NEVER reset between spells (leak across casts); (c) duration-spell hit-FAIL consumes NO energy while resist/fail consumes half; (d) only the FIRST between-round slot can fire per round (GoTo skips the rest); (e) full-run average divides by N+1 (rounds+1) — off-by-one preserved; (f) dodge is rolled BEFORE AC; (g) MobIsEvil is set but never read |
| IsSpellResisted | RandomNumber | Audited-ported | IsSpellResisted | Gate order verbatim; MR cap at 196 (roll 1–200, +4 floor) |
| CalcResistedDamage | — | Audited-ported | CalcResistedDamage | PIN: mr<50 non-anti BOOSTS damage by (50−mr)% (anchor: 100 dmg @ MR 40 → 110). DEVIATION (doc'd, 1e-14 noise): elemental resist math decimal vs VB6 Double→CCur |
| Apply_GMUD_DiminishingReturns (private) | — | Audited-ported | private ApplyGmudDiminishingReturns | PIN: byte-identical DUPLICATE of modMMudFunc GMUD_DiminishingReturns (:2504) — class-local copy is faithful to source duplication, kept private |
| ResetActiveAtkSpell / ResetActiveBetweenSpell | — | Audited-ported | same names | Slot-state resets verbatim |
| ResetActiveSpells | ResetActive* | Audited-ported | ResetActiveSpells | Walks both arrays |
| ResetValues | — | Audited-ported | ResetValues | PIN: OMITS AtkDuration, AtkHitSpellName, BetweenRoundDuration, CombatLogMaxRounds — they survive reset (anchored) |
| ProgressBar subs (Init/Update/Hide) + DoEvents + privHandleError | frmMain UI | Dropped (doc'd) | — | UI-only; no logic |
| Dynamic-calc early stop | — | Audited-ported | RunSim | Stops when successive averages differ < DynamicCalcDifference; DEVIATION (doc'd, noise): decimal-first division order |

Wave5SimTests.cs: 25 anchors, ScriptedRandom throws on queue exhaustion so every
anchor also validates the EXACT number of random draws. Two anchor fixes during
authoring were test-side (next_attempt fires after successful attacks → attempt
3 never draws), not port defects. Suite 570/570.

## modExpPerHour.bas — 28 procedures (Phase 1d — COMPLETE, 28/28 accounted; runtime surface fully ported, dev harness dropped with note)

Module survey: 5,002 lines, pure Double math. ONLY external dependency is
bGreaterMUD (→ IGameEngineRules). Debug plumbing (bDebugExpPerHour,
DebugLogPrint, cephC_DebugPrint, cephB_DebugLog, DebugPrintExpHrGlobals)
DROPPED — never affects math. VB6-Private helpers made public for testing.

| Proc | deps | status | C# location | pins / notes |
|---|---|---|---|---|
| module constants (public + private ceph*) | — | Ported (wave 1) | ExpHourMath consts | Verbatim incl. DEFAULT_CEPH* knob defaults |
| tExpPerHourInfo | — | Ported (wave 1) | Model/ExpHourTypes.cs ExpPerHourInfo | Verbatim field set |
| MinDbl / MaxDbl / ClampDbl / SafeDiv | — | Ported (wave 1) | ExpHourMath | SafeDiv default 0; exact-0 divisor test |
| cephC_Ceil | Fix | Ported (wave 1) | CephCCeil | Fix-based ceiling; negative → Fix (toward 0) |
| cephC_EstimateMoveSecs | — | Ported (wave 1) | CephCEstimateMoveSecs | Density < 0.05 only; dens^0.2 clamped [0.9, 1.1] |
| cephB_Saturate / SmoothStep / Lerp / MulBlend / BandWeight | — | Ported (wave 1) | ExpHourMath | SmoothStep degenerate edge0==edge1 → step at edge |
| cephB_CalcOverkill | Exp | Ported (wave 1) | CephBCalcOverkill | Logistic; hp≤0 → 1; spell mult 1.35, caps 1.06/1.18 |
| cephB_CalcDensity | SafeDiv | Ported (wave 1) | CephBCalcDensity | avgWalk fallback when either input ≤ 0 |
| cephB_CalcTravelLoopSecs | smoothing helpers | Ported (wave 1) | CephBCalcTravelLoopSecs | PIN: dens ≥ 5 sets scarcity with (coef − 0.03), but the ELSE route-band branch unconditionally recomputes scarcity from the BASE coefficient — the reduction only survives inside the 12–16-lair/walk ≥ 5 discrete band. Both regimes anchored |
| cephB_ApplySlackWindow | SafeDiv, MaxDbl | Ported (wave 1) | CephBApplySlackWindow | ref params; tick lengths stay PARAMETERS (VB6 signature shadows the module constants by name) |
| cephD_OverkillFrac | cephC_Ceil | Ported (wave 1) | CephDOverkillFrac | First-round opener vs avg; spill/lastDmg clamped [0,1] |
| cephA_InCombatMPFrac | ClampDbl | Ported (wave 1) | CephAInCombatMpFrac | PIN: both branches start 0.26; clamps never bite |
| cephA_CalcHPRecoveryRounds | consts | Ported (wave 1) | CephACalcHpRecoveryRounds | q-elasticity g piecewise (0.6 floor/cap); passive-in-combat coefficient 0.3; nMobs Integer default 0 → clamp 1; restHP floor 1; rtc = 0 exact-compare gates the RTK derivation |
| IsMobKillable | **modMain.bas**; bGreaterMUD, *_MOB_HPREGEN_ROUNDS | Ported (wave 1) | ExpHourMath.IsMobKillable | rules.MobHpRegenRounds (stock 18 / GMUD 6). PINS: nMobTotalHP dead store dropped w/ note; nCharTotalHP Long = Double regen credit banker's-CLng (anchored 133.33 → 133); rtk > 720 hard False; regen scales by rtk/window when fight shorter than window (stock-vs-GMUD divergence anchored) |
| ceph_ModelA | wave-1 helpers, cephA_* helpers, knobs, bGreaterMUD | Ported (wave 2, full 1,076-line read) | ExpHourModels.ModelA.cs CephModelA | SEAMS: knobs → ExpHourKnobs DTO (VB6 globals are 0.0 until UI init; XP/Mana/route multiplies are UNGUARDED — knob 0 zeroes output, defaults here = UI value 1.0); bGreaterMUD → rules.MobHpRegenRounds. PINS: (a) top-of-function tuner block (coef 0.25) is DEAD — movement branch recomputes from DENSITY_COEF 0.2 before first use; (b) bail-out tests nTotalLairs = 0 EXACTLY — default −1 with regenTime 0 slips through (anchored); (c) boss shortcut skips the XP knob (commented out in VB6) and returns zeroed fractions (anchored with knob 2×); (d) regenAtten smoothstep argument is UNCLAMPED — regenRatio > 0.9 goes negative-polynomial so extreme mob regen can push atten above 1; only the 0.55 floor exists; (e) cephA_CalcHPRecoveryRounds receives nNumMobs Double into an Integer param — banker's CInt; (f) Select Case qRatio band 0.3 To 0.7 inclusive both ends; (g) restAsManaEq NEVER assigned (2025-11-10 patch commented the computation out) — rest→mana credit path is dead, T_M2 ≡ T_M1; (h) spawn gating consumes the STALE pre-overlap recoveryDemandFrac against the OVERWRITTEN post-overlap recoveryDemandTime (anchored: cluster scenario shows TimeRecovering ≠ HP frac); (i) roomsPerPool < 1 floor exists only in the walk-credit recompute, not the first computation; (j) nRegenTime += 0.25 after the boss shortcut; (k) EPH banker's-Rounds BEFORE the XP knob; (l) negative NMove = cluster-limit flag, negative NAttackTime = surprise-worse flag (dispatcher decodes). Anchors from an independent VB6-text Python replica (9 scenarios incl. stock-vs-GMUD attenuation divergence) |
| ceph_ModelB | wave-1 helpers (Saturate/SmoothStep/Lerp/MulBlend/BandWeight/CalcOverkill/CalcDensity/CalcTravelLoopSecs/ApplySlackWindow), TextUtils.RoundUp, knobs, bGreaterMUD | Ported (wave 3, full 1,015-line read) | ExpHourModels.ModelB.cs CephModelB | SEAMS: knobs → ExpHourKnobs (Move/Dmg/Mana/XP multiplies UNGUARDED — anchored knob-0 zeroing); bGreaterMUD → rules.MobHpRegenRounds. PINS: (a) defaults differ from A — nNumMobs 1, nRTK 1, nCharMana is Integer (short); RTK derivation fires only on explicit 0; (b) boss shortcut PRECEDES the lairs-0/regen-0 bail-out (reverse of A) and skips the XP knob; (c) lairs < 0 → bInstant 22 s micro-floor; reaching it implies regenTime = 0 so the respawn gate never stacks; (d) NO +0.25 regen-time slack (unlike A); (e) single-mob RTK derivation is banker's Round(hp/dmg, 1) — anchored 55/60 → 0.9; multi-mob is ceil-0.5 × mobs; (f) caster effRTK = Max(Min(rtk·0.78, RoundUp), 0.74) — 0.74 floor anchored via NRtc; (g) near-one-shot melee cut uses RAW nRTK, not effRTK; (h) regenAtten uses the CLAMPED SmoothStep helper (unlike A's unclamped inline) — but the one-shot pOneShot Lerp saturates savings to effRTK, making attenuation irrelevant for true one-shots (anchored: only PARTIAL surprise diverges stock-vs-GMUD, to the 0.55 floor); (i) hpWalkEq_B/walkForHPPassiveEq computed but never consumed — dead, dropped with note; (j) dmgPerRoundCore divides nMobDmg by effective lair rounds (A treats mob damage as already per-round); (k) manaCostLoop/totalRounds/manaGain/killSecsAll/medNeeded/poolCredit are function-scoped and stay 0 for ApplySlackWindow when the mana block is skipped; (l) EPH NOT rounded — fractional output anchored (8181.8181…); (m) nSlowdownTime is the effRTK/nRTK − 1 RATIO, not a time share (anchored 0.25); (n) bBackstabLess negates nAttackTime. Anchors from an independent VB6-text Python replica (10 scenarios incl. mid-band MB1–MB5 micro-patch case, meditation display overlap, rest→mana relabel, stock-vs-GMUD partial-surprise divergence) |
| cephC_BuildCombatProfile / cephC_BuildCycleProfile / ceph_ModelC | CephCCeil, CephCEstimateMoveSecs, cephC_* constants, knobs, bGreaterMUD | Ported (wave 4, full read :217–1155) | ExpHourModels.ModelCD.cs (CephCBuildCombatProfile / CephCBuildCycleProfile / CephModelC + public CephCCombatProfile/CephCCycleProfile types) | SEAMS: knobs → ExpHourKnobs (Move/Dmg/Mana/XP UNGUARDED); bGreaterMUD → rules.MobHpRegenRounds. PINS: (a) nWalkSpeed default 1 (A/B use 1.25); (b) CombatProfile mob regen only when approxRTK ≥ 6 and regen > 0, regenPerRound keeps the SEC_PER_ROUND/(rounds·SEC_PER_ROUND) expression verbatim, avgDmgForSeq floored at 10% of avgDmgNorm (stock-vs-GMUD anchor: RTC 13 vs 24); (c) deterministic overkill recompute uses avgDmgNorm NOT avgDmgForSeq; (d) ONE-SHOT OVERKILL BUG preserved — mobHP ≤ firstDmg forces hpBeforeLast = 0 → OverkillFrac = 1.0 (anchored exactly; cephD_OverkillFrac's header names this bug); (e) boss regen is HOURS (·3600); (f) effMobDmgPerRound = mobDmg · cephC_Rest_KNOB(0.8) · DmgKnob unguarded; (g) threshold < 0 zeroes drains AND regens → trivial path; (h) useHP true with regen even without damage (macro loop then runs all 200 lairs); (i) slack only when non-boss and both rests ≤ 0: 0.2·attackSecs/RTK scaling attack+move equally; (j) spawn supply = lairs·60/regen with NO +0.25 (Model D has it); (k) non-boss EPH ·1.05·XpKnob, boss gets NEITHER, never rounded; (l) pressure split recomputes mob dmg WITHOUT Rest_KNOB (drain calc includes it) — inconsistent vintages preserved; (m) surprise-worse flag (surp < charDMG < mobHP/mobs) negates nAttackTime; (n) S* strings via "0.0"/"0.00" masks (banker's, matching pinned Format$ semantics). Anchors from an independent VB6-text Python replica (9 scenarios incl. surprise-chance/min-damage tails, unlimited slack, mob-regen stock/GMUD pair) |
| ceph_ModelD | cephD_OverkillFrac, CephCCeil, cephC_* rest fracs, cephD_* constants, CephBSmoothStep, VbRuntime.CLng | Ported (wave 4, full read :1205–1543) | ExpHourModels.ModelCD.cs CephModelD | SEAMS: knobs → ExpHourKnobs (Dmg/Mana/Move/XP UNGUARDED). PINS: (a) perMobDmg reconstruction UNDOES GetLairInfo's transforms using the RAW nRTK parameter (·(N+1)/(2N) when N > 1, ÷nRTK when nRTK > 1) — not the derived RTK_avg; (b) RTK derivation only on explicit ≤ 0, Model A-style ceil-0.5, floor 1; (c) round loop: killedBefore = Int((r−1)/RTK), mobsAlive floored at 1, final round weighted by frac = RTC − (ceil−1); (d) unlimited (threshold < 0) zeroes drains but the per-kill overhead STILL applies at full strength (recFrac 0 → gate 1 → 1.5 s·mobs) inside attack time — anchored; (e) spawn supply divides by (regenTime + 0.25) — unlike Model C; (f) boss regen is HOURS and boss EPH skips the (otherwise unguarded) XP knob; (g) serialized rest→meditate macro-cycle with meditate at 0.5 efficiency + passive; (h) heavy-hit rest relief 0.35·SmoothStep(0.25, 0.5, hpLost/HPmax); (i) nMobHPRegen is accepted but entirely unused by Model D; (j) restRelief/overheadSecs stay 0 on the boss path; (k) slowdown = (RTK−1)/RTK clamped [0,1]. Anchors from the same independent replica (5 scenarios) |
| CalcExpPerHour (dispatcher) | all four models, IsMobKillable, TextUtils.AutoAppend/RoundUp, VbRuntime.Round/CLng | Ported (wave 5, full read :1543–1777) | ExpHourModels.Dispatcher.cs CalcExpPerHour + ExpHourModelSelection | SEAMS: bGlobal_cephModelA..D/ShowAll/RecoveryOnly → ExpHourModelSelection (VB6 globals default False until UI init — DTO mirrors that; .All convenience provided). PINS: (a) killability gate passes (nMobHP − nSurpriseDMG) Double into IsMobKillable's Long mob-HP parameter — banker's CLng coercion — and nCharHPRegen Long → Integer range-checked; (b) nCharMana is Long here but B/C/D take Integer — checked-cast overflow above 32,767 preserved; (c) **RTC DOUBLE-ACCUMULATE BUG** — nRTC is added TWICE per model in the accumulation loop AND divided by nCount TWICE in the nCount > 1 block (with intermediate 2-dp banker's rounds): a SINGLE enabled model reports DOUBLED RTC (anchored: single Model B melee shows 16.0 vs the model's 8), multi-model gets Round(Round(2Σ/n, 2)/n, 2); (d) nCount > 1: EPH banker's 0 dp, all fractions 2 dp; nCount ≤ 1: raw pass-through, ShowAll forcibly disabled, per-model strings cleared; (e) ShowAll EPH strings: > 1M → "#,#.00"+"M", > 1000 → "#,#.0"+"K", else RoundUp "#,#" ("0" if ≤ 0), "/" glue, wrapped " (…)" only at nCount > 1; (f) text thresholds: attack ∈ (0,1), slowdown > 0.01 < 1, overkill > 0.01 AND charDMG < 9,999,999, rec/hp/mana/move > 0.01, roam > 0.04; (g) cluster / backstab-worse suffixes appended with single-space glue after decoding ANY model's negative nMove/nAttackTime; (h) -8675309 sentinel replaced by nullable results (semantics identical). Anchors from a composed replica (the three anchored model replicas + dispatcher logic from the VB6): 5 scenarios incl. all-four ShowAll string assembly verified verbatim |
| RunAllSimulations / LoadSimRows / ParseNum / SplitFields / FormatResult (+ cephB_DebugLog / cephC_DebugPrint / DebugPrintExpHrGlobals debug printers) | none (dev-only) | Dropped with note | — | VB6-side calibration harness over a hardcoded tSimRow table plus debug log printers; never called from the application runtime path. The C# parity suite (627 tests) supersedes the harness role. Revisit only if §8.2 VB6-runtime CSV goldens surface a need |

## modMMudDatabase.bas / Mme.Data — 95 procedures (Phase 1e — IN PROGRESS, 24/95 accounted; plus GetDamageOutput + PopulateCharacterProfile + GetAbilityStatSlot from modMain.bas below)

**Storage decision (Phase 1e session 1):** the C# application reads SQLite natively via Microsoft.Data.Sqlite (System.Data.OleDb dependency removed from Mme.Data.csproj — Windows-only ACE/Jet is out). A one-shot Jackcess-based converter (jar, same toolchain as the realm-builder tooling) turns any mmud .mdb export into the app's SQLite database. The MDB is never opened by the app itself.

| Procedure | Depends on | Status | C# location | Pins / notes |
|---|---|---|---|---|
| LairInfoType (Public Type) | — | Ported | Mme.Data/Model/LairInfo.cs | Currency → decimal, Long → long, Integer → short. NAMING PIN: nMaxRegen carries the MOB COUNT per lair inside GetLairInfo (group-index part 3; avgAlive = (n+1)/(2n), RTC = RTK·n) — name kept verbatim |
| tDamageOutput (Public Type, modMain.bas) + eDefenseFlags (4 members) | — | Ported | Mme.Core/Model/DamageTypes.cs | Currency damage fields; enum values verbatim (Undead 1, Animal 2, Living 4, AntiMag 8). Consumed by GetLairInfo today; produced by GetDamageOutput (future wave) |
| GetLairInfoIndex / SetLairInfo | dictLairInfo/colLairs() | Ported | Mme.Data/LairInfoService.cs (GetOrCreate private + SetLairInfo) | VB6 parallel array + Scripting.Dictionary collapsed to one keyed dictionary — index numbers never escape the module. SetLairInfo gates: damage sextet + config only persisted when sGlobalAttackConfig non-empty; nMaxRegen only when > 0 (anchored) |
| GetLairInfo | GetDamageOutput (SEAM), GetPreCalculatedMonsterDamage + nMonsterDamageVsChar UBound (SEAM), CalcCombatRounds (ported), frmMain filter controls (SEAM), bStartup (SEAM), sGlobalAttackConfig (SEAM), LAIR_FLAG_RATIO, VbRuntime.Val/CInt/Round | Ported (wave 1, read :562–830) | Mme.Data/LairInfoService.cs GetLairInfo + LairQueryOptions/LairDamageRequest | SEAMS at exact VB6 boundaries: options == null ⇔ bStartup skip (cache copy, RTK 1, RTC = mob count); UseCharacter ⇔ chkGlobalFilter; PartySize ⇔ optMonsterFilter(1)/txtMonsterLairFilter(0) clamped 1–6; DamageProvider ⇔ GetDamageOutput(0, AC, DR, MR, Dodge, flags, 100, immu, magic, , BS, RCOL..RWAT); PartyDamage/PartyDamageUpperBound ⇔ GetPreCalculatedMonsterDamage + UBound guard. PINS: (a) NPossSpawns never copied from cache — returned field always 0; (b) anti-magic flag threshold is mobs/2 while undead/living/animal use LAIR_FLAG_RATIO 0.9; (c) DamageOutput Currency fields coerce into Long locals — banker's, anchored 24.5→24 / 31.5→32 / 10.5→10; (d) both -9999 sentinels → local damage forced to 9,999,999 (one-shot RTK) with NO write-back and cache damage untouched; (e) write-back (SetLairInfo + config stamp) only on fresh compute at party 1; (f) mitigation Round(sum/count, 1) lands in a LONG field — 1-dp banker's then CLng (anchored 41/3 → 13.7 → 14), then Currency − Long re-coerce; (g) CalcCombatRounds receives nAvgDmgLair (Currency) through the Long mobDamage param — banker's; charHealth/regen omitted, numMobs hard 1; (h) RTK ∈ (0,1) floors to 1, only RTK > 1 multiplies nAvgDmgLair (Round ·, 1); (i) avgAlive = (mobs+1)/(2·mobs) division applies REGARDLESS of RTK path when mobs > 1 and dmg > 0 — anchored 30/(4/6) = 45.0 on the zero-damage path — this is the transform ceph_ModelD undoes; (j) zero-damage path zeroes RTK AND RTC despite the copy phase seeding 1/mobs. 8 test methods, transform-chain expectations computed against the anchored CalcCombatRounds with identical coercions |
| RemoveOutliers / GetMedian / GetMedianAbsDev / GetStdDev / QuickSort | — (pure) | Ported (wave 2, read :392–562) | Mme.Core/Formulas/StatsMath.cs | Pure stats stack pulled forward to Core. PINS: cutoff = 3·MAD around median, MAD == 0 falls back to SAMPLE stddev (n−1; n<2 → 0; squares via ^/Pow); if EVERY element is an outlier the array is left UNTOUCHED (VB6 comments out the empty ReDim); median copies + QuickSorts (odd → middle, even → mean of two middles); QuickSort = in-place Hoare with middle pivot (first+last)\2. Feeds GetLairAveragesFromLocs (future wave) |
| DICT_ModeFromCounts / DICT_AddToCount / DICT_BumpCount | Scripting.Dictionary | Ported (wave 2, read :1370–1414) | Mme.Data/LairLoader.cs (internal statics) | Mode = highest count, ties prefer the HIGHER level; bestCount init −1 (a zero-count key can win); outcome order-independent. AddToCount skips delta 0. Dictionary<long,long> replaces Scripting.Dictionary |
| LoadLairInfo | tabLairs/tabMonsters walks, GetLairInfo("")/SetLairInfo, DICT helpers, nNMRVer (Info."NMR Version" via ExtractNumbersFromString — Phase 1a port), MAJ_THRESH_PCT, bGreaterMUD | Ported (wave 2, read :988–1372) | Mme.Data/LairLoader.cs Load(MmeDatabase, IGameEngineRules, LairInfoService) | Rebuilds the lair cache from Lairs+Monsters. PINS: (a) GMUD subtracts 0.5 from AvgDelay at LOAD; (b) Lairs table has NO PossSpawns column — never loaded (matches GetLairInfo never copying it); (c) ability scan slots 0–9 skip Abil==0; resists 3/5/65/66/147 accumulate into Longs (banker's per add); 28/139 levels CLng'd into level-count dicts, monsters with NO such field bump level 0; 51 anti-magic count, 78 animal, 109 clears living default (living assumed TRUE); (d) accuracy slots 0–4 for AttType 1..3: NMR ≥ 1.8 → CLng(Round(AttTrue%-i)), else cumulative differencing of Att%-i where prevCum updates for ALL types 1–3 (type 2 consumes state but never folds); negative pct clamps 0; zero pct skips AFTER the prevCum update; only types 1,3 fold into per-monster uniq buckets (same-acc slots merge); (e) averages use VB6 \ (operands banker's to Long, division truncating toward zero — anchored −4 avg fire resist on lair 11-1-5); divide only when accumulator ≠ 0; (f) hasMajority (MAJ_THRESH_PCT 51) computed but DECORATIVE in VB6 — both branches assign the plurality; dropped with note, nAccyMajority = plurality-or-0; (g) SetLairInfo's gates skip the damage sextet (empty config) and nMaxRegen (0) on the loader's write-back. Integration anchors from an INDEPENDENT Python replica over the converted stock 1.11p: 421 lairs; lair 10-10-15 (11 mobs, 1 animal, 11 living, plurality acc 65, max 85) and lair 11-1-5 (2 undead, BS 21, RCOL 54, RFIR −4, RLIT 12, plurality 60, max 90) |
| GetLairAveragesFromLocs | GetLairInfo, InstrCount + CalcAverageNonZero (modMain.bas — ported into TextUtils/StatsMath this wave), RegExpFindv2/AutoAppend/RemoveDuplicateNumbersFromString/RoundUp (Phase 1a), RemoveOutliers stack, DICT helpers, MAJ_THRESH_PCT, GetDamageOutput (SEAM), nNMRVer, sGlobalAttackConfig (SEAM) | Ported (wave 3, read :164–392) | Mme.Data/LairInfoService.Averages.cs GetLairAveragesFromLocs(sLoc, nNmrVer, options) | sLoc = the monster's "Summoned By" field verbatim (frmMain :25437; the VB6 body reads the SAME field for PossSpawns — one parameter here). PINS: (a) nPossSpawns = InstrCount(sLoc, "Group:") BEFORE the version gate — NMR < 1.83 exits with only that set ("Group(lair):" doesn't substring-match "Group:"); (b) regex \[([\d\-]+)\]\[(\d+)\]Group\(lair\): (\d+)/(\d+); submatch 1 feeds GetLairInfo as the mob count; lair/room groups captured but unused; (c) nLairs = TOTAL match count — guard-skipped lairs still count in every divisor AND leave a ZERO in the walk array that participates in RemoveOutliers before CalcAverageNonZero drops it (anchored [4,0] → 4.0); (d) most averages Round(sum/nLairs) banker's 0 dp; RTC/RTK/nMaxRegen/nAvgDelay at 1 dp; nMobs UNROUNDED (Currency 4-dp assignment); Num* counts RoundUp (ceiling — anchored (2+4)/2 → 3); walk Round(avgNonZero(filtered), 1); (e) accuracy majority-of-majorities: per-lair pluralities > 0 vote, winner needs ≥ 51% of votes or result 0 — the threshold is LIVE here, unlike LoadLairInfo (anchored 3-way split → 0); (f) nMaxRegen floors to 1; nPossSpawns += nLairs; sGroupIndex = the WHOLE sLoc (frmMain cache key); sMobList deduped concat; (g) immune check only when any immu/magic/undead/antiMag/living/animal > 0 — provider called WITHOUT the BSDefense/resist tail (VB6 optionals omitted → zeros, asserted on the request) and ONLY −9998 zeroes the avg/surprise triples; other returns discarded. 6 synthetic hand-traced tests + real-DB integration (shade #15 Monsters-tab Exp/Hr equals an independent chain recomputation) |
| ItemHasAbility (specific-ability mode) | tabItems seek; AbilityEffectsCharStats (only the −1 mode — DEFERRED with it) | Ported (wave 4, read :3235–3313) | Mme.Data/MmeDatabase.cs GetItemAbilityValue(item, ability, gmud) | Sentinel −31337 = missing item / ability not present. GMUD maps abilities 2/7/22 onto ArmourClass/DamageResist/Accy columns BEFORE the Abil scan (no exit — a matching Abil slot could still accumulate per the VB6 comment); first matching Abil slot returns 0 + AbilVal. The nAbility = −1 "any functional ability" mode needs AbilityEffectsCharStats — deferred together |
| GetSpellMinDamage / GetSpellMaxDamage (:4527–4691, near-identical pair) | tabSpells seek (resolver seam); self-recursion via abil 151 | Ported (wave 5, read line-by-line) | Mme.Core/Formulas/SpellDamageMath.cs (statics over Func<long, SpellRecord?>) | PINS: damage mode counts abils 1/8/17 and skips 18; heals mode counts 18 AND 8 — drain counts BOTH ways; non-zero AbilVal on a counted slot = FIXED override that skips clamp AND scaling (GoTo multi_calc) with the LAST matching slot winning, so any chain recursion then gets the UNCLAMPED cast level; clamp runs when (not forMonster) OR castLevel = 0 — monsters with positive levels skip it but monster castLevel 0 still clamps to ReqLevel; scaling = base + Fix((inc/incLvls)·castLevel) (Double ÷ then truncation); energy multi-cast is players-only: energyRem 0 → 1000, −EnergyCost, floor 1; extra casts need energyRem ≥ 143 AND EnergyCost ≥ 143; no-chain multiplier result·Fix(energyRem/EnergyCost) gated EnergyCost ≤ 500; chain recursion passes the MUTATED castLevel + DECREMENTED energyRem and DROPS bHealsInstead (heal chains flip to damage mode); nEnergyRem/bForMonster/bHealsInstead are implicit ByRef in VB6 — every call site audited (literals/field reads/never re-read) → observably inert, ported by value. Real anchors: spell 18 (15/22/45/32 vs monster-62 at level 50; EnergyCost 1000 → rem floors to 1, no multi-cast), 285 meteor swarm (E250: 80 @ default energy, 40 @ 600, 20 forMonster), 35 poison bolt (chain target 1366 has NO damage abils → fired chain adds 0), 263 dragonfire (EnergyCost 0 blocks its chain forever), 54 vampiric touch (drain: 8 in both modes), 409 constriction (fixed AbilVal 2 at any level). Synthetic: heals-drop + mutated-level recursion (57), last-slot-wins + unclamped recursion (149) |
| GetSpellDuration (:4691–4733) | tabSpells seek (resolver seam) | Ported (wave 5) | SpellDamageMath.GetSpellDuration | Same clamp semantics as the damage pair; Dur + Fix((DurInc/DurIncLVLs)·castLevel). Anchors: spell 18 → 0, spell 409 → 20 |
| SpellDoesDamage (:4733–4786) | tabSpells seek; self-recursion via abil 151 | Ported (wave 5) | SpellDamageMath.SpellDoesDamage | bNotDuration excludes Dur > 0 or (DurIncLVLs > 0 AND DurInc > 0) up front; abils 1/8/17 → true; PIN: the abil-151 recursion does NOT forward bNotDuration — the chained spell's duration is never checked (no stock spell exercises this; proven with a synthetic resolver). Anchors: 18 true/true, 409 true/false(notDuration) |
| GetMaxLevel (:155–164) | gAvgLevelMaxAllStats global (param seam); RoundUpTo5 (Phase 1a) | Ported (wave 5) | SpellDamageMath.GetMaxLevel | 255 unless RoundUpTo5(avg) STRICTLY exceeds 255 (256 → 260; 253 → 255 because RoundUpTo5(253) = 255 is not > 255). Previously only externalized as maxLevel = 255 on wave-3 consumers |
| GetCurrentSpellMinMax (:3941–4067) | — | Ported in **wave 3** (SpellMath.GetCurrentSpellMinMax) — wave 5 added an independent CROSS-CHECK | Mme.Core/Formulas/SpellMath.cs | Wave 5 re-read the VB6 and re-derived expectations from scratch, briefly produced a duplicate implementation, then caught it via this ledger — duplicate deleted, and the fresh expectations were retargeted at the wave-3 port as 5 cross-check tests (numeric scaling, formula strings + maxLevel pull at level 0, bonus Fix + "+N%" suffix on min/max only, all-static special path, override suppression). All pass — mutual validation of both readings |
| PullSpellEQ (:4067–4527, ~460 lines) | GetCurrentSpellMinMax, quartet, ListView UI, bQuickSpell/nSpellNest globals | **Ported (session 48)** | Mme.Data/SpellEqService.cs (GetSpellEq) | Returns a display STRING (ListView population, "(click)" nesting via bQuickSpell, nSpellNest > 19 guard "to infinity and beyond?"). Head pins recorded: bUseLevel = bCalcLevel with global-filter level pull, Cap/ReqLevel clamp (skipped for monsters), nLevel < 1 → ReqLevel, then GetCurrentSpellMinMax. Parity here is string formatting — belongs with the detail panel |
| GetSpellManaCost (:1909) | tabSpells seek | Ported (wave 6) | MmeDatabase.GetSpellManaCost | PIN: 0 < EnergyCost ≤ 500 multiplies ManaCost by Fix(1000/EnergyCost) — the round-total mana of a multi-cast. Anchors: spell 18 (E1000 → plain 4), 285 (E250 → 16) |
| GetClassCombat (:2229) | tabClasses seek | Ported (wave 6) | MmeDatabase.GetClassCombat | CombatLVL − 2; default/miss 1. Trailing `GetClassCombat = 1` after Exit Function is unreachable dead code — noted, dropped. Anchors: Warrior 6 → 4, Priest 3 → 1 |
| GetClassStealth (:2542) | tabClasses seek; frmMain global-filter fallback (EXTERNALIZED) | Ported (wave 6) | MmeDatabase.GetClassStealth | Abil 103 scan. The nNum = 0 path read the UI class combo — externalized: callers resolve first, 0 → false. Anchors: Thief true, Warrior false |
| GetItemStrReq (:3698) / IsTwoHandedWeapon (:3907) | tabItems seek | Ported (wave 6) | MmeDatabase.GetItemStrReq / IsTwoHandedWeapon | StrReq or 0; two-handed = ItemType 1 AND WeaponType ∈ {1, 3}. Anchors: greatsword 347 (WT3, StrReq 45) true, cutlass 342 (WT2) false |
| PopulateCharacterProfile (**modMain.bas** :5178–5380) | GetClassCombat/GetClassStealth/GetSpellManaCost/GetItemStrReq/IsTwoHandedWeapon/ItemHasAbility (all this-or-prior waves), CalcEncumbrancePercent + CalculateAccuracy (Phase 1b), rules.MovementSpeed + rules.BackstabAccuracy (Phase 1b engine rules), frmMain controls + session globals (SEAM: CharacterSheetState DTO — field-by-field control map in the DTO comments), GetMaxLevel (wave 5) | Ported (wave 6, read line-by-line) | Mme.Data/CharacterProfileService.cs (+ CharacterSheetState) | ByRef semantics preserved — the passed profile is MUTATED and unset fields RETAIN caller values (tested). PINS: (a) party pulls from the filter boxes only when the option is on AND size > 1, then clamps 1–6; (b) branch select: character when !bForceNoChar AND ((useCharacter AND party < 2) OR bForceUseChar), else party (> 1 AND !bForceNoParty), else generic maximum; (c) WalkSpeed = Round(MovementSpeed(encumPct, quicknessTag)/1000, 2) only when encumPct > 0 else 1.25; MeditateRate gated on bGlobalAttackUseMeditate; SpellOverhead = healCost + bless/6; SpellAttackCost only in spell modes with a spell; (d) bCalcAccy: surprise always; GMUD also on bash/smash UI-vs-requested mismatches; otherwise accuracy comes from the label tag; (e) surprise weapon ElseIf chain (> 0 → it; < 0 → fists; backstab-on-and-set → BS weapon; backstab-off OR BS-weapon-0 → main hand); when the resolved weapon ≠ main hand: ItemHasAbility 22/116 each clamped at 0 (absorbing the −31337 sentinel), off-hand SUBTRACTION when the new weapon is two-handed with an off-hand present, then main-hand contributions subtract — the BS delta MUTATES tChar.nPlusBSaccy before BackstabAccuracy; (f) GMUD adds AccyItems into the backstab plus-normal sum, stock does not; (g) party branch: HP < 1 WRITES 1 BACK to the UI box (DTO field mutated to mirror), HP/HPRegen ×party, HitMagic pair 9999, MA skills forced 1 for punch–jumpkick; (h) generic branch: Level = GetMaxLevel, combat 5, 255s, accy 999, PlusBSaccy 999, both stealth flags, HP 10000/regen 5%, threshold from txtMonsterDamage when NMR < 1.83 else heal value (+ spell cost in spell modes); (i) tail clamps: threshold 0..9999999, HP/HPRegen floor 1, mana/regen/overhead/attackCost/encum%/accuracy floor 0 cap 9999999 (encum% cap 100); (j) all Double→Integer/Long UI coercions are VB6 assignment coercions → VbRuntime banker's CInt/CLng, not truncation. 7 tests: thin-lookup anchors, generic branch + NMR threshold gate, party multipliers + write-back pin + retained-fields probe, character mapping + walk speed + label-accuracy path, surprise accuracy main-hand (relative vs direct BackstabAccuracy) and different-weapon (adjustment + mutation, fully relative), tail clamps. WIRED: ProfileRequest gained ForceCharacter (GetDamageOutput passes bForceCharacter at all four sites); the VM's ManualAttackOptions profile source is now the REAL Populate with a default sheet — manual mode hits the generic-maximum branch exactly as frmMain |
| GetAbilityStatSlot (**modMain.bas** :8395–8548) | GetAbilityStats (display string — dropped with note, caller owns text) | Ported (RC wave, read line-by-line) | Mme.Core/Formulas/AbilityStatSlots.cs | Ability → lblInvenCharStat slot routing; 10x codes = main-stat boxes (101 STR/102 AGI/103 CHA/104 INT/123 HEA/124 WIS). PINS: 9/21/38/39/41/72/87 commented-out or absent → −1 (72's comment: "repurposed to hitmagic 2025.10.05"); 22/105/106 all → accuracy 10; 179/180 → same slots as 40/37; 28 deliberately unrouted (the equipment loop special-cases 28+142 on the weapon slot) |
| ItemIsUsableByChar (**frmMain.frm** :40382–40566) | txtGlobalLevel/cmbGlobalClass/cmbGlobalAlignment/txtGlobalMinLVL (→ parameters), nEquippedItem equipped-exemption (→ callback) | Ported (session 22) + real-db anchors | Mme.Data/ItemUsabilityService.cs | Fast-path lvl≥999+Any+Any; abil 135/136 level window; abil 59 ClassOk bypasses type checks; abil 28 magical vs class-abil 51 anti-magic; align abils 97/98/112 + not-align 110/111/113; ClassRest any-nonzero-no-match fails; armour classArmourType ≥ itemType + stock 1H-class shield denial (Worn 12); weapon-type switch w/ Any groupings, 8=all, Staff(9) hardcodes items 68/100; RaceRest deliberately unchecked (VB6 quirk) |
| RefreshCharBless (**frmMain.frm** :38129–38268) + bless combo population (:21700–21810) | cmbCharBless selections (→ blessSpells input), tooltips (dropped), SpellIsUsable list filter (not yet ported — list matches filter-off VB6) | Ported (session 25) + real-db anchors | Mme.Data/BlessService.cs | Level clamp [ReqLevel..Cap>0]; nAvgCast CLng banker's; AbilVal 0 → cast average; abil 7 /10 Round 1dp; stock accy highest-bless-wins vs GMUD accumulate; bless BLUR sees wornArmourType=0 (VB6 resets tracker pre-call → stock always Fix(/2)); mana Σ Round(Mana/(dur·3),3)×5×6 Round 2 |
| SaveCharacter / LoadCharacter INI (**frmMain.frm** :38725–38935) | window/filter/compare keys (preserved verbatim as extras, not consumed) | Ported (session 25) + round-trip tests | Mme.Data/CharacterFileService.cs | "Widsom" key typo preserved on write (tolerated as "Wisdom" on read); slot keys Head..Everywhere = cmbEquip 0..19; IM_CARRIED "n|qty," cap 50; unknown keys round-trip |
| CalcCharacterStats (**frmMain.frm** :26986–28230) + InvenCalcEncum :26935 + AdjMainStatBonus :26947 + GetRaceHPBonus (modMMudDatabase :2051) + Get/CalcQuickAndDeadlyBonus (modMMudFunc :4485/:4527) + InvenAddEquip :26857 | carried items / bless / manual adjustments / quest checkboxes / HideCharStats / tooltips+colors (→ neutral inputs or dropped-with-note) | Ported (session 23) + real-db anchors | Mme.Data/EquipmentStatsService.cs, Mme.Data/ItemUsabilityService.cs (EquipSlotCatalog) | Encum computed FIRST; worn-armour tracker reads ItemType not ArmourType (:27417, suspected VB6 bug, preserved); class/race DR ÷10 vs AC raw; stock accy highest-wins, GMUD accumulates; hitmagic GMUD max-wins vs stock cumulative + weapon fold; BLUR GMUD (100−enc%)→Fix/10→Round/10,1 vs stock armour-class divisors 4/3/2; item AC/DR (v·qty)/10 Round(,1) banker's; STR max Fix((s−50)/10) no GMUD negatives, min Fix((s−100)/10) stock ×2 floor 0; stock crit cap 75 pre-add, floor 1, GMUD +(5−combat), >40 diminishing 40+Fix((c−40)/3) cap 99 DISPLAY-ONLY; walk GMUD Round 2dp vs stock 0dp banker's; GMUD SC>150 → +GmudGetSpDmgMultiplierFromSc; 20 equip slots, fingers/wrists pair-populated, weapons→16 |
| GetDamageOutput (**modMain.bas** :4825–5176) + eAttackRestrictions + nGlobalAttackTypeMME enum + nChar*VsMonster cache arrays + ClearSavedDamageVsMonster | CalculateAttack/CalculateSpellCast/CalculateResistDamage (all ported, Phase 1b), tCharacterProfile (ported), ItemHasAbility (this wave), PopulateCharacterProfile (SEAM — frmMain UI reader, own wave), module attack globals + frmMain controls (SEAM: AttackConfig), tabMonsters/tabSpells/tabItems seeks (MmeDatabase) | Ported (wave 4, read line-by-line) | Mme.Data/DamageOutputService.cs (+ AttackConfig, ProfileRequest, DamageVsMonsterCache, AttackRestrictions, MmeAttackType) | PINS: (a) the nSpeedAdj PARAMETER is overwritten to 100 immediately — dead signature; (b) bForceCharacter forces party 1; (c) cache HIT needs config match AND avg ≥ 0 AND first ≥ 0 — negative sextets are stored but never hit (recomputed every call); config mismatch clears + stamps; (d) single-monster load OVERWRITES passed AC/DR/MR (BSDefense only NMR ≥ 1.83; dodge from abil 34; living default true unless 109; Seek miss → sentinel struct out); (e) surprise only at party 1 + type > oneshot + backstab flag; weapon pick: BackstabWeapon > 0 → it, == 0 → main hand if any, < 0 → fists; weapon-magic gate: ItemHasAbility 28 plus 142 (the 142 read guards > −500 vs the −31337 sentinel), GMUD MAXes contributions where stock ADDS; fists use nHitMagicNonWeapon only; fail → surprise −9998/swings 0; surprise avg = AvgHit + AvgExtraHit but surprise MIN = MinDmg + AvgExtraSwing (the swing average, not extra-hit); (f) party/manual: physical routes through CalculateAttack specifyDamage (party 1 passes vsDr; party > 1 zeroes vsDr and pre-subtracts DR·partySwings, swings clamped 1–6); magical adds CalculateResistDamage(M, MR, 2, True, False, antiMag); first = min = avg; swings floors to 1 only when (first + avg) > 0; manual accuracy = char tag when chkGlobalFilter else 9999; (g) weapon mode with main hand 0 computes NOTHING after passing the gate — sentinels stand; (h) spell validity: immunity gate (immu == 0 or castLevel > immu) wraps only flag determination; restriction match is an ElseIf CHAIN — only the FIRST set restriction (undead → animal → living) is tested; unknown flags resolve from the spell's Abil 23/80/108; ≤ AR001_None → valid; (i) first-round finalize: attack swings > 0 → tAttack.nFirstRoundDamage, else spell nMinCast > 0 → first = avg. Monster-stats map cached per service instance (read-only data; VB6 recordset reuse equivalent). 8 tests: oneshot, manual relative parity (vs direct CalculateAttack + CalculateResistDamage), party DR-zeroing/swing-clamp, single-monster overwrite + cache hit/clear (profile-source call counting), negative-cache-never-hits, weapon-magic gate ±, undead-restriction chain vs real spell 18, immunity gate |

## modItemParse.bas — (Phase 1f — NOT STARTED)
