MMUD-MIMIC — BETA 28 CHANGES-ONLY PACKAGE
=========================================
Baseline: beta 27 (last full source release).
Beta 28 = rename "MMUD Explorer" -> "MMUD-Mimic" + new treasure-chest icon.
No engine/data/logic changes; test suite unchanged at 858/858.

CHANGED FILES (9, all modifications):
  TEXT (covered by beta28.patch):
    PORT_LOG.md
    FEATURE_CENSUS.md
    README.md
    src/Mme.App/App.xaml.cs
    src/Mme.App/MainWindow.xaml
    src/Mme.App/MainWindow.xaml.cs
    src/Mme.App/Mme.App.csproj
  BINARY (NOT patchable — copy these two files over directly):
    src/Mme.App/app-icon.ico   (5-size treasure chest: 16/32/48/64/256)
    src/Mme.App/app-icon.png   (256px window icon)

APPLY
-----
A) From a clean beta-27 checkout:
     git apply beta28.patch        # the 7 text files
     # then copy the 2 icon files over (they can't live in a text patch):
     cp src/Mme.App/app-icon.ico src/Mme.App/app-icon.png  <your-repo>/src/Mme.App/
   (`git apply --check beta28.patch` verified clean.)
B) Or just extract this whole zip on top of a beta-27 checkout — the 9
   files are laid out at their real repo-relative paths and overwrite
   exactly what changed (icons included).

WHAT CHANGED
------------
* App renamed to MMUD-Mimic: window title, crash/error dialogs, the
  character save/open dialog filters, and the executable (now
  MMUD-Mimic.exe via <AssemblyName>). Namespaces / .sln / project
  filenames are intentionally unchanged (internal only).
* Docs (README/census) name the port MMUD-Mimic while still calling the
  original VB6 tool "MMUD Explorer".
* New pixel-art treasure-chest icon (.ico with 5 crisp sizes + 256 PNG).
