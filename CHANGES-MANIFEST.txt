MMUD-MIMIC — BETA 30 CHANGES-ONLY PACKAGE
=========================================
Baseline: beta 29. 8 files, all text (7 modified + 1 NEW).

VERIFIED: applying beta30.patch to a clean beta-29 checkout builds and
passes the full suite (883/883) on its own, so the delta is complete.
The patch includes the new-file addition, so `git apply` creates
SpellEqService.cs for you — nothing to copy by hand.

NEW FILE:
  src/Mme.Data/SpellEqService.cs      (PullSpellEQ + GetAbilityStats port)
MODIFIED:
  src/Mme.App.ViewModels/MainViewModel.Browse.cs   (Effects + References)
  src/Mme.App.ViewModels/MainViewModel.Map.cs      (Summon/Spell jumps)
  tests/Mme.Core.Tests/Wave18AttackPanelTests.cs   (+16 tests)
  docs/PARITY_LEDGER.md   (3 rows: Deferred -> Ported (session 48))
  PORT_LOG.md, FEATURE_CENSUS.md, README.md

APPLY
-----
A) From a clean beta-29 checkout:  git apply beta30.patch
B) Or extract this zip over a beta-29 checkout (real repo-relative paths).

WHAT CHANGED (beta 30) — PullSpellEQ, the last flagged task
-----------------------------------------------------------
Ported VB6 modMMudDatabase.bas :: PullSpellEQ (:4067-4527) and
modMMudFunc.bas :: GetAbilityStats (:2242-2330). These were the last two
entries carrying "Deferred (Phase 3 VM)" in the parity ledger.

* FOUR separate accumulators (detail / EndCast / flags / removes) because
  the OG assembles them in a fixed order: detail, EndCast, energy-cost
  tail, flags, "(@lvl N):" prefix, "for N rounds", "-- RemovesSpells(...)".
* Nested EndCast spells render RECURSIVELY (with the OG's 19-deep guard
  and the abil-164 "N% " chance prefix).
* DR in tenths; the no-header ability set; RemovesSpells trailer;
  energy-cost Fix(1000/cost) "xN times/round" (suppressed when nested);
  non-magical spells rewrite "Damage(-MR)" -> "Damage".
* minMaxDamageOnly ("min:max[:dur]" / "0:0:0") and quickSpell ("(click)")
  modes ported.
* GetAbilityStats: the 148 special case where a textblock whose action is
  only "cast N" clauses displays those spells' EQ instead of the tb ref.
* The OG's ListView of clickable refs is externalized as a line list,
  formatted to match existing jump patterns:
    "Teleport: <room> (map/room)", "Execute: Textblock N [TB N]",
    "Summon: <monster> (N)", "Spell: <spell> (N)"
  NavigateFromLine's monster regex now accepts "Monster:" OR "Summon:",
  and a new "Spell:" branch + SelectSpellInGrid gives spell->spell jumps.

DIVERGENCES (logged): bQuickSpell is a parameter but not bound to a UI
toggle (OG used it for grid tooltips); bPercentColumn dropped (it only
picked a ListView column, meaningless once the list is externalized); the
OG's post-ability record re-Seek is unnecessary here.
