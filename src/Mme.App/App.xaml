<Application x:Class="Mme.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
  <Application.Resources>
    <!-- Implicit themed control styles — App scope so EVERY window
         (tool windows included) inherits them (S45 audit: they lived
         in MainWindow.Resources, leaving tool windows unthemed). -->
    <Style TargetType="ScrollBar">
      <Setter Property="Background" Value="{DynamicResource ThPanel}"/>
      <Setter Property="Width" Value="12"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ScrollBar">
            <Grid Background="{DynamicResource ThPanel}">
              <Track x:Name="PART_Track" IsDirectionReversed="True">
                <Track.DecreaseRepeatButton>
                  <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0" Focusable="False"/>
                </Track.DecreaseRepeatButton>
                <Track.IncreaseRepeatButton>
                  <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0" Focusable="False"/>
                </Track.IncreaseRepeatButton>
                <Track.Thumb>
                  <Thumb>
                    <Thumb.Template>
                      <ControlTemplate TargetType="Thumb">
                        <Border Background="{DynamicResource ThBorder}" CornerRadius="3" Margin="2"/>
                      </ControlTemplate>
                    </Thumb.Template>
                  </Thumb>
                </Track.Thumb>
              </Track>
            </Grid>
            <ControlTemplate.Triggers>
              <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Height" Value="12"/>
                <Setter Property="Width" Value="Auto"/>
                <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="False"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="GroupBox">
      <Setter Property="Foreground" Value="{DynamicResource ThHeader}"/>
      <Setter Property="BorderBrush" Value="{DynamicResource ThBorder}"/>
      <Setter Property="FontWeight" Value="Bold"/>
    </Style>
    <Style TargetType="TextBlock">
      <Setter Property="Foreground" Value="{DynamicResource ThLabel}"/>
    </Style>
    <Style TargetType="Label">
      <Setter Property="Foreground" Value="{DynamicResource ThLabel}"/>
    </Style>
    <Style TargetType="TextBox">
      <Setter Property="Background" Value="{DynamicResource ThInputBg}"/>
      <Setter Property="Foreground" Value="{DynamicResource ThInputFg}"/>
      <Setter Property="BorderBrush" Value="{DynamicResource ThBorder}"/>
      <Setter Property="CaretBrush" Value="{DynamicResource ThInputFg}"/>
      <Setter Property="SelectionBrush" Value="{DynamicResource ThGridSel}"/>
          <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>
    <Style TargetType="CheckBox">
      <Setter Property="Foreground" Value="{DynamicResource ThLabel}"/>
      <Setter Property="VerticalContentAlignment" Value="Center"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="CheckBox">
            <StackPanel Orientation="Horizontal" Background="Transparent">
              <Border x:Name="box" Width="13" Height="13"
                      Background="{DynamicResource ThInputBg}"
                      BorderBrush="{DynamicResource ThBorder}" BorderThickness="1"
                      VerticalAlignment="Center">
                <Path x:Name="check" Data="M 1 5 L 5 9 L 11 1" Stroke="{DynamicResource ThBright}"
                      StrokeThickness="2" Visibility="Collapsed"/>
              </Border>
              <ContentPresenter Margin="5,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
            <ControlTemplate.Triggers>
              <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="check" Property="Visibility" Value="Visible"/>
              </Trigger>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="box" Property="BorderBrush" Value="{DynamicResource ThTabHover}"/>
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{DynamicResource ThBtnDisabled}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style TargetType="Button">
      <Setter Property="Background" Value="{DynamicResource ThBtnBg}"/>
      <Setter Property="Foreground" Value="{DynamicResource ThBright}"/>
      <Setter Property="BorderBrush" Value="{DynamicResource ThBorder}"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="bd" Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="1" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="bd" Property="Background" Value="{DynamicResource ThBtnHover}"/>
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="bd" Property="Background" Value="{DynamicResource ThBtnPress}"/>
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{DynamicResource ThBtnDisabled}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
      <Setter Property="Padding" Value="6,2"/>
    </Style>
    <Style TargetType="ComboBox">
      <Setter Property="Background" Value="{DynamicResource ThInputBg}"/>
      <Setter Property="Foreground" Value="{DynamicResource ThInputFg}"/>
      <Setter Property="BorderBrush" Value="{DynamicResource ThBorder}"/>
      <Setter Property="SnapsToDevicePixels" Value="True"/>
      <Setter Property="MinHeight" Value="22"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ComboBox">
            <Grid>
              <ToggleButton x:Name="toggle" Focusable="False" ClickMode="Press"
                            IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                <ToggleButton.Template>
                  <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="face" Background="{DynamicResource ThInputBg}"
                            BorderBrush="{DynamicResource ThBorder}" BorderThickness="1">
                      <Grid>
                        <Grid.ColumnDefinitions>
                          <ColumnDefinition Width="*"/>
                          <ColumnDefinition Width="18"/>
                        </Grid.ColumnDefinitions>
                        <Path Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center"
                              Data="M 0 0 L 4 4 L 8 0 Z" Fill="{DynamicResource ThBright}"/>
                      </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                      <Trigger Property="IsMouseOver" Value="True">
                        <Setter TargetName="face" Property="BorderBrush" Value="{DynamicResource ThTabHover}"/>
                      </Trigger>
                      <Trigger Property="IsChecked" Value="True">
                        <Setter TargetName="face" Property="Background" Value="{DynamicResource ThBtnHover}"/>
                      </Trigger>
                    </ControlTemplate.Triggers>
                  </ControlTemplate>
                </ToggleButton.Template>
              </ToggleButton>
              <ContentPresenter Margin="6,2,22,2" HorizontalAlignment="Left" VerticalAlignment="Center"
                                Content="{TemplateBinding SelectionBoxItem}"
                                ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                IsHitTestVisible="False"/>
              <Popup x:Name="PART_Popup" Placement="Bottom" AllowsTransparency="True"
                     IsOpen="{TemplateBinding IsDropDownOpen}" PopupAnimation="Fade"
                     StaysOpen="False">
                <Border Background="{DynamicResource ThInputBg}"
                        BorderBrush="{DynamicResource ThBorder}" BorderThickness="1"
                        MinWidth="{TemplateBinding ActualWidth}"
                        MaxHeight="{TemplateBinding MaxDropDownHeight}">
                  <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                  </ScrollViewer>
                </Border>
              </Popup>
            </Grid>
            <ControlTemplate.Triggers>
              <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{DynamicResource ThBtnDisabled}"/>
                <Setter Property="Opacity" Value="0.6"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <!-- dropdown popup: dark list, black selection shadow so item
         colors (align red/gray/white) stay readable -->
    <Style TargetType="ComboBoxItem">
      <Setter Property="Background" Value="{DynamicResource ThInputBg}"/>
      <Setter Property="Foreground" Value="{DynamicResource ThGridFg}"/>
      <Setter Property="Padding" Value="4,2"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ComboBoxItem">
            <Border x:Name="bd" Background="{TemplateBinding Background}"
                    Padding="{TemplateBinding Padding}">
              <ContentPresenter/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsHighlighted" Value="True">
                <Setter TargetName="bd" Property="Background" Value="{DynamicResource ThSel}"/>
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{DynamicResource ThBtnDisabled}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style TargetType="ListBox">
      <Setter Property="Background" Value="{DynamicResource ThGridBg}"/>
      <Setter Property="Foreground" Value="{DynamicResource ThGridFg}"/>
      <Setter Property="BorderBrush" Value="{DynamicResource ThBorder}"/>
    </Style>
    <Style TargetType="Menu">
      <Setter Property="Background" Value="{DynamicResource ThBg}"/>
      <Setter Property="Foreground" Value="{DynamicResource ThBright}"/>
    </Style>
    <!-- S45: MenuItem fully RE-TEMPLATED. Property triggers alone lose
         to Aero2's template-internal highlight (the hardcoded light-blue
         wash that made dark-theme menu hover unreadable) — the default
         template's own trigger paints named elements directly, which
         outranks any style setter. One template covers all four roles. -->
    <Style TargetType="MenuItem">
      <Setter Property="Foreground" Value="{DynamicResource ThLabel}"/>
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="MenuItem">
            <Border x:Name="bd" Background="{TemplateBinding Background}"
                    Padding="7,4" SnapsToDevicePixels="True">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="16"/>
                  <ColumnDefinition Width="*"/>
                  <ColumnDefinition Width="Auto"/>
                  <ColumnDefinition Width="14"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="check" Text="&#x2713;" FontWeight="Bold"
                           Visibility="Hidden" VerticalAlignment="Center"/>
                <ContentPresenter Grid.Column="1" ContentSource="Header"
                                  RecognizesAccessKey="True"
                                  VerticalAlignment="Center"/>
                <TextBlock Grid.Column="2" Opacity="0.65" Margin="16,0,0,0"
                           VerticalAlignment="Center"
                           Text="{TemplateBinding InputGestureText}"/>
                <TextBlock x:Name="arrow" Grid.Column="3" Text="&#x25B6;"
                           FontSize="8" Visibility="Collapsed"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Center"/>
                <Popup x:Name="PART_Popup" Placement="Right"
                       IsOpen="{TemplateBinding IsSubmenuOpen}"
                       AllowsTransparency="True" Focusable="False"
                       PopupAnimation="Fade">
                  <Border Background="{DynamicResource ThPanel}"
                          BorderBrush="{DynamicResource ThBorder}"
                          BorderThickness="1" Padding="0,2">
                    <ItemsPresenter/>
                  </Border>
                </Popup>
              </Grid>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="Role" Value="TopLevelHeader">
                <Setter TargetName="PART_Popup" Property="Placement"
                        Value="Bottom"/>
              </Trigger>
              <Trigger Property="Role" Value="SubmenuHeader">
                <Setter TargetName="arrow" Property="Visibility"
                        Value="Visible"/>
              </Trigger>
              <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="check" Property="Visibility"
                        Value="Visible"/>
              </Trigger>
              <Trigger Property="IsHighlighted" Value="True">
                <Setter TargetName="bd" Property="Background"
                        Value="{DynamicResource ThSel}"/>
                <Setter Property="Foreground"
                        Value="{DynamicResource ThSelFg}"/>
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground"
                        Value="{DynamicResource ThBtnDisabled}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <!-- S45: RadioButton was never styled — black text on the dark
         panel (the unreadable By Mob / By Lair report) -->
    <Style TargetType="RadioButton">
      <Setter Property="Foreground" Value="{DynamicResource ThLabel}"/>
      <Setter Property="VerticalContentAlignment" Value="Center"/>
      <Style.Triggers>
        <Trigger Property="IsEnabled" Value="False">
          <Setter Property="Foreground"
                  Value="{DynamicResource ThBtnDisabled}"/>
        </Trigger>
      </Style.Triggers>
    </Style>
    <Style TargetType="ContextMenu">
      <Setter Property="Background" Value="{DynamicResource ThPanel}"/>
      <Setter Property="Foreground" Value="{DynamicResource ThLabel}"/>
      <Setter Property="BorderBrush" Value="{DynamicResource ThBorder}"/>
    </Style>
    <Style TargetType="Separator">
      <Setter Property="Background" Value="{DynamicResource ThBorder}"/>
    </Style>
    <Style TargetType="StatusBar">
      <Setter Property="Background" Value="{DynamicResource ThBg}"/>
      <Setter Property="Foreground" Value="{DynamicResource ThHeader}"/>
    </Style>
    <Style TargetType="ToolTip">
      <Setter Property="Background" Value="{DynamicResource ThTipBg}"/>
      <Setter Property="Foreground" Value="{DynamicResource ThTipFg}"/>
      <Setter Property="BorderBrush" Value="{DynamicResource ThBorder}"/>
      <Setter Property="FontFamily" Value="{DynamicResource ThFont}"/>
    </Style>

    <Style x:Key="TabBtn" TargetType="RadioButton">
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="RadioButton">
            <Border x:Name="bd" BorderBrush="{DynamicResource ThBorder}" BorderThickness="1"
                    Background="{DynamicResource ThTabBg}" Padding="10,3" Margin="0,0,-1,0">
              <ContentPresenter HorizontalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="bd" Property="Background" Value="{DynamicResource ThTabSelBg}"/>
                <Setter Property="Foreground" Value="{DynamicResource ThTabSelFg}"/>
              </Trigger>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="bd" Property="BorderBrush" Value="{DynamicResource ThTabHover}"/>
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{DynamicResource ThBtnDisabled}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
      <Setter Property="FontWeight" Value="Bold"/>
      <Setter Property="Foreground" Value="{DynamicResource ThLabel}"/>
    </Style>

    <Style TargetType="DataGrid">
      <Setter Property="AutoGenerateColumns" Value="False"/>
      <Setter Property="IsReadOnly" Value="True"/>
      <Setter Property="EnableRowVirtualization" Value="True"/>
      <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling"/>
      <Setter Property="CanUserAddRows" Value="False"/>
      <Setter Property="HeadersVisibility" Value="Column"/>
      <Setter Property="RowHeight" Value="17"/>
      <Setter Property="FontSize" Value="11"/>
      <Setter Property="GridLinesVisibility" Value="All"/>
      <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource ThGridLine}"/>
      <Setter Property="VerticalGridLinesBrush" Value="{DynamicResource ThGridLine}"/>
      <Setter Property="Background" Value="{DynamicResource ThGridBg}"/>
      <Setter Property="RowBackground" Value="{DynamicResource ThGridBg}"/>
      <Setter Property="AlternatingRowBackground" Value="{DynamicResource ThGridAlt}"/>
      <Setter Property="Foreground" Value="{DynamicResource ThGridFg}"/>
      <Setter Property="BorderBrush" Value="{DynamicResource ThBorder}"/>
      <Setter Property="SelectionMode" Value="Single"/>
      <Setter Property="SelectionUnit" Value="FullRow"/>
      <Setter Property="EnableRowVirtualization" Value="True"/>
    </Style>

    <Style TargetType="DataGridColumnHeader">
      <Setter Property="Background" Value="{DynamicResource ThHeadBg}"/>
      <Setter Property="Foreground" Value="{DynamicResource ThHeader}"/>
      <Setter Property="BorderBrush" Value="{DynamicResource ThBorder}"/>
      <Setter Property="BorderThickness" Value="0,0,1,1"/>
      <Setter Property="Padding" Value="4,2"/>
      <Setter Property="FontWeight" Value="Bold"/>
    </Style>
    <Style TargetType="DataGridRow">
      <Style.Triggers>
        <Trigger Property="IsSelected" Value="True">
          <Setter Property="Background" Value="{DynamicResource ThGridSel}"/>
          <Setter Property="Foreground" Value="{DynamicResource ThGridSelFg}"/>
        </Trigger>
      </Style.Triggers>
    </Style>
    <Style TargetType="DataGridCell">
      <Setter Property="BorderThickness" Value="0"/>
      <Style.Triggers>
        <Trigger Property="IsSelected" Value="True">
          <Setter Property="Background" Value="{DynamicResource ThGridSel}"/>
          <Setter Property="Foreground" Value="{DynamicResource ThGridSelFg}"/>
        </Trigger>
      </Style.Triggers>
    </Style>
  </Application.Resources>
</Application>
