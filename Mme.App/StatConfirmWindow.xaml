<Window x:Class="Mme.App.StatConfirmWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirm Modified Stats" Width="430" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
  <StackPanel Margin="10">
    <TextBlock TextWrapping="Wrap" Margin="0,0,0,8"
               Text="These stats were marked '*' in your paste — the game shows them WITH bless/equipment bonuses. Enter your actual base values (suggestions subtract your current calculated bonuses):"/>
    <ItemsControl x:Name="LstStats">
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <Grid Margin="0,2">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="90"/>
              <ColumnDefinition Width="110"/>
              <ColumnDefinition Width="70"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding Label}" VerticalAlignment="Center"/>
            <TextBlock Grid.Column="1" VerticalAlignment="Center"
                       Foreground="Gray" Text="{Binding PastedText}"/>
            <TextBox Grid.Column="2" Text="{Binding Base,
                     UpdateSourceTrigger=PropertyChanged}"/>
          </Grid>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                Margin="0,10,0,0">
      <Button Content="Apply" Width="90" Margin="0,0,8,0" IsDefault="True"
              Click="Apply_Click"/>
      <Button Content="Keep pasted values" Width="130" IsCancel="True"/>
    </StackPanel>
  </StackPanel>
</Window>
